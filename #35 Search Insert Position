class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        count = 0
        for i in range(len(nums)):
            if nums[i] < target:
                count += 1
            elif nums[i] == target:
                return i
        return count
